# 贡献指南

1. 从最新 `main` 创建 `agent/<description>` 或功能分支。
2. 新行为必须先添加能够正确失败的 Pester 测试。
3. 每个 PR 只处理一个可独立审查的里程碑。
4. 不得提交真实项目、绝对用户路径、设备序列号、缓存、日志或凭据。
5. 提交前运行全部测试和 Skill 快速验证。

任何涉及文件删除的变化必须证明：校验失败不删源、目标路径受限、清理需要独立确认。
