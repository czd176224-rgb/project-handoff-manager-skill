---
name: project-handoff-manager
description: Windows 项目管家。用于开始或继续已有项目文件夹、暂停项目并停止其开发进程、将完整项目归还到 T9、从 T9 借出到本机、更新项目交接信息，以及跨电脑继续项目。涉及项目迁移、释放项目 CPU/内存、项目缓存或离线依赖随项目携带时使用。
---

# 项目管家

## 使用原则

1. 每次先运行 `scripts/project_manager.ps1 -Action menu`，向用户显示四项操作。
2. 任何操作前，先展示将执行的内容、涉及的精确路径和预期结果。
3. 复制、校验和清理必须分阶段执行；校验成功前不得删除源副本。
4. 不读取、不修改、不迁移 Codex 桌面版数据库或对话数据库。
5. 不迁移登录凭据、令牌、Cookie 或系统级软件本体。
6. 项目文件夹实扫结果是事实来源，登记文件只作为索引。

## 四项操作

1. 开始或继续当前项目：对已有文件夹自动补充项目管理结构，读取交接报告并检查环境。
2. 暂停当前项目：先列出可证明属于项目的进程，用户确认后停止，并更新交接信息。
3. 将当前项目归还到 T9：更新交接、缓存和离线依赖清单，完整复制、校验，再单独确认清理本机副本。
4. 从 T9 借出项目到本机：完整复制、校验、检查环境，再单独确认清理 T9 副本。

## 当前版本边界

当前 `0.1.0` 为安全骨架。菜单可以运行，真实项目扫描、暂停、归还和借出操作将在后续里程碑实现。未实现的操作必须返回“未执行”，不得更改文件。
